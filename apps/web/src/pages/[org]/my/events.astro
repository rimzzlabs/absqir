---
import { translatorFor } from "@absqir/i18n";
import { MyEventsPage } from "@/components/my/my-events-page";
import DashboardLayout from "@/layouts/dashboard-layout.astro";

const { activeMembership } = Astro.locals;

if (!activeMembership) return Astro.redirect("/", 302);

const t = translatorFor(Astro.locals.locale);
---

<DashboardLayout title={t("shell:titles.myEvents")}>
  <MyEventsPage client:load locale={Astro.locals.locale} orgSlug={activeMembership.slug} />
</DashboardLayout>
