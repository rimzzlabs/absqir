---
import { QrDisplay } from "@/components/qr-display/qr-display";
import Layout from "@/layouts/layout.astro";

const { id } = Astro.params;

if (!id) {
  return Astro.redirect("/", 302);
}
---

<Layout title="QR — absqir">
  <QrDisplay client:load sessionId={id} />
</Layout>
