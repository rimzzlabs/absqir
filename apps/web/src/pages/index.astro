---
import { HomePage } from "@/components/home/home-page";
import DashboardLayout from "@/layouts/dashboard-layout.astro";

const { user, activeMembership } = Astro.locals;

// A member has no overview; their home is their own sessions.
if (activeMembership?.role === "member") {
  return Astro.redirect("/my/sessions", 302);
}
---

<DashboardLayout title="Home">
  <HomePage client:load userName={user?.name ?? ""} />
</DashboardLayout>
