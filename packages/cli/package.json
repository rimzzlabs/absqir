{
  "name": "absqir",
  "version": "0.0.0",
  "description": "Operator CLI for self-hosting absqir, the QR attendance system",
  "license": "MIT",
  "type": "module",
  "bin": {
    "absqir": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@absqir/config": "workspace:^",
    "@types/node": "^26.5.0",
    "tsdown": "^0.23.0",
    "vitest": "^5.0.0"
  }
}
